<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.studiobeu.yaniv">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icone_yanive"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icone_yanive_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.PlayerSelect" />
        <activity android:name=".activity.GameActivity" />
        <activity android:name=".activity.YanivActivity" />
        <activity android:name=".activity.EditPlayerActivity"></activity>
    </application>

</manifest>